# Specify

## Rule

Use `specs/baseline.md` as the root baseline specification.

## Active Spec Kit Feature

Use `specs/001-ai-native-baseline/spec.md` as the generated Spec Kit feature
specification for planning.
