# Quickstart: Unified Documentation

To see the consolidated project instructions, build commands, and configurations, check the newly unified root `README.md`.

## Running the Verification

1. Inspect the root directory to verify that only `README.md` and `AGENTS.md` remain.
2. Read `README.md` to see the consolidated sections for running:
   - Spring Boot backend (port 8080)
   - FastAPI RecommendationSystem (port 8000)
   - Flask API Server (port 5000)
   - Gemini Web2API (port 8081)
   - Flutter Frontend app
